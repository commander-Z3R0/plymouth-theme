name: Build and Release Debian Package

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: write

env:
  THEME_VERSION: 1.0.0  

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y dpkg-dev

      - name: Make postinst executable
        run: |
          chmod +x theme-sirros/DEBIAN/postinst

      - name: Build Debian package
        run: |
          mkdir -p build/sirros-theme
          cp -R theme-sirros/DEBIAN build/heme-sirros/
          cp -R theme-sirros/usr build/theme-sirros/
          cd build
          dpkg-deb --build theme-sirros

      - name: Rename package with version
        run: |
          cd build
          mv theme-sirros.deb theme-sirros-v${{ env.THEME_VERSION }}.deb

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: sirros-theme-deb
          path: build/theme-sirros-v${{ env.THEME_VERSION }}.deb

      - name: Create GitHub Release (even without tag)
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Release main build #${{ github.run_number }}
          files: build/theme-sirros-v${{ env.THEME_VERSION }}.deb
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
